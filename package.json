{
  "name": "template-saas-packages",
  "version": "1.0.0",
  "private": true,
  "description": "Shared asset library: theme-able UI registry (@template/ui), retro UI kit (@pxlkit/*), external-link-cms. Consumed by product repos via git subtree.",
  "scripts": {
    "build": "pnpm -r build"
  },
  "pnpm": {
    "overrides": {
      "@types/react": "19.2.2",
      "@types/react-dom": "19.2.2"
    }
  }
}
