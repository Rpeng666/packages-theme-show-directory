import type { Section } from "@template/ui";
import * as React from "react";

export const props = {
  icon: "wand",
  label: "Wand",
  active: false,
  onClick: () => {},
  tone: "cyan" as const,
};
