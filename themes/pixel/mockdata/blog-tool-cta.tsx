import type { Section } from "@template/ui";
import * as React from "react";

export const props = {
  cta: {
    headline: "Enjoying the article?",
    sub: "Try the tool that wrote the breakdown.",
    href: "#",
    ctaLabel: "Open the tool",
  },
};
