import type { Section } from "@template/ui";
import * as React from "react";

export const props = {
  currentColor: "green",
  colorInfo: { key: "green", color: "#51cf66" },
  progressPercentage: 42,
  t: (key: string) => key,
};
