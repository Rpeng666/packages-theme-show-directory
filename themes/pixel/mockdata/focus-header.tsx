import type { Section } from "@template/ui";
import * as React from "react";

export const props = {
  title: "Focus mode",
  onBack: () => {},
  onSettings: () => {},
  currentColor: "green",
  t: (key: string) => key,
};
