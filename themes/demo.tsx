/**
 * Demo props entry — aggregated from themes/default/mockdata/*.
 * Each default-theme block has its own mockdata file exporting `props`;
 * this barrel re-exports the aggregate so app code imports stay stable.
 */
export { DEMO_PROPS } from "./default/mockdata";
