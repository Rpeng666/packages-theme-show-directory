import type { Section } from "@template/ui";
import * as React from "react";

export const props = {
  label: "Opacity",
  value: 65,
  min: 0,
  max: 100,
  step: 1,
  unit: "%",
  onChange: () => {},
};
