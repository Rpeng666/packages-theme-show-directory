import type { Section } from "@template/ui";
import * as React from "react";

export const props = {
  children: <span className="text-lg">✦</span>,
  size: 44,
  radius: 12,
  tone: "brand" as const,
};
