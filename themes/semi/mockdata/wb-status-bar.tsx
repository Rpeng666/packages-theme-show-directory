import type { Section } from "@template/ui";
import * as React from "react";

export const props = {
  zoom: 100,
  canvasW: 1920,
  canvasH: 1080,
  mouseX: 320,
  mouseY: 240,
  t: (key: string) => key,
};
