import type { Section } from "@template/ui";
import * as React from "react";

export const props = {
  value: 30,
  min: -100,
  max: 100,
  step: 5,
  accentColor: "#4ecdc4",
  onChange: () => {},
};
