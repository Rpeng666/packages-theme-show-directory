import type { Section } from "@template/ui";
import * as React from "react";

export const props = {
  children: <span className="text-xs font-medium uppercase tracking-wider">Eyebrow</span>,
  tone: "brand" as const,
};
