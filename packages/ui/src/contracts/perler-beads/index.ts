/** Perler-beads shared contracts — self-contained types, no app-layer deps. */
export type {
  PerlerMappedPixel,
  PerlerPaletteColor,
  PerlerColorReplaceState,
  PerlerPaletteSelections,
  PerlerColorSystem,
} from './types'
export type { GridDownloadOptions } from './download-types'
