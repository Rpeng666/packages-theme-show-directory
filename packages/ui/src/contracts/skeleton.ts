import type * as React from 'react'

/**
 * Skeleton contract — a plain loading placeholder div.
 * Both themes accept the same props as a native div (width/height via
 * className utilities or inline style).
 */
export type SkeletonProps = React.ComponentProps<'div'>
