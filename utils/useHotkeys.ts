import { useWorkbenchHotkeys } from "@template/ui/themes/raycast";

/** 转发层：raycast 主题 useWorkbenchHotkeys（原 app utils/useHotkeys，已注册进主题） */
export default useWorkbenchHotkeys;
export { useWorkbenchHotkeys };
